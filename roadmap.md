# Roadmap

- [ ] Framework swap to TanStack Start (in progress; migration lifecycle started)
  - [ ] Preflight scan summary posted
  - [ ] Steps 1–9 (scaffolding, deps merge, routes, SEO→head(), tracking hook, build gates)
  - [ ] Post-swap: confirm legacy redirect paths (list old→new explicitly for user)
  - [ ] Post-swap: verify all three roles (developer, owner, editor) can log in and session survives reload — real browser test, not just compile
  - [ ] Contact page as first reviewed page: curl raw server HTML head, visual parity, test lead insert, typecheck
- Nothing published.
